// S1. 사용법 — 한 화면에 담는다 (PRD §6 S1)
import GuideScreen from '@/front/screens/GuideScreen';

export const metadata = { title: '사용법 · 그루뽑기' };

export default function Page() {
  return <GuideScreen />;
}
